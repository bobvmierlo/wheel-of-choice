flask>=3.0
# optional — only needed for push notifications (see README):
pywebpush>=2.0
# optional — only needed for calendar busy/free in date polls (see README):
icalendar>=6.0
recurring-ical-events>=3.0
